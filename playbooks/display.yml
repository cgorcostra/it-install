---
- name: Install using official repo
  hosts: all

  tasks:
    - name: "your public key for git"
      debug:
        msg: "{{current_user.public_key}}"
      when: current_user.changed == true  and current_user.skipped == false